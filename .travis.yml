language: java
jdk:
  - oraclejdk8
services:
   - redis-server
before_install:
    - chmod +x maybe-release.sh
install:
    - mvn install -Dmaven.javadoc.skip=true -B -V
script:
   - ./maybe-release.sh
after_success:
  - bash <(curl -s https://codecov.io/bash)
